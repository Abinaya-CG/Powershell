pipeline {
  agent optim 
  stages {
    stage('version') {
      steps {
        sh 'pwsh --version'
      }
    }
    stage('hello') {
      steps {
        sh 'pwsh hello.ps1'
      }
    }
  }
  post{
      always{
        sh 'pwsh --version'
      }
   }
}
