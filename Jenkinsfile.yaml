pipeline {
  agent any
  stages {
    stage('version') {
      steps {
      agent{
       node {
        label 'optim' 
       }
      }
        sh 'pwsh --version'
      }
    }
    stage('hello') {
      steps {
        sh 'pwsh hello.ps1'
      }
    }
  }
  post{
      always{
        sh 'pwsh --version'
      }
   }
}
