pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        script{
          
          echo "${params.Application}"
          echo "${params.JobName[]}"
        }
      }
    }
    stage('hello') {
      steps {
        powershell 'Write-Output "HI!!"'
      }
    }
  }
}
