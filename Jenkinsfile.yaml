pipeline {
  agent  {
   node {
        label 'optim' 
       }
  }
  stages {
    stage('version') {
      steps {
        powershell '$PSVersionTable.PSVersion'
        
        script{
          def mapList=[]
          def headers=[]
          def file = readFile "optim_mapping.csv"
          file.readLines().eachWithIndex { row, index ->
           if (index == 0) { 
             def headers[0],headers[1],headers[2],headers[3],headers[4]=row.split(',')
           }
           else {
             def tmpMap = [:]
             def cells = row.split(',').eachWithIndex { cell, cellIndex ->
             echo cellIndex
             tmpMap[headers[cellIndex]] = cell
           }
           mapList << tmpMap
           }
          echo mapList
        }
      }
    }
    }
    stage('hello') {
      steps {
        powershell 'Write-Output "HI!!"'
      }
    }
  }
}
