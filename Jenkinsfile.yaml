pipeline {
  agent {
   node {
        label 'optim' 
       }
  }
  stages {
    stage('version') {
      steps {
        script{
          
          echo "${params.Application}"
          echo ""
        }
      }
    }
    stage('hello') {
      steps {
        powershell 'Write-Output "HI!!"'
      }
    }
  }
}
