pipeline {
  agent  {
   node {
        label 'optim' 
       }
  }
  stages {
    stage('version') {
      steps {
        script{
          powershell -Command($PSVersionTable)
        }
      }
    }
    stage('hello') {
      steps {
        script{
        powershell $ScriptPath -Appname -SchemaName -JobName -Path
      }
    }
  }
}
}
