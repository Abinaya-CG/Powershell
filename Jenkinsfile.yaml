pipeline {
  agent  {
   node {
        label 'optim' 
       }
  }
  stages {
    stage('version') {
      steps {
        powershell("$PSVersionTable.PSVersion")
      }
    }
    stage('hello') {
      steps {
      }
    }
  }
  post{
      always{
        sh 'pwsh --version'
      }
   }
}
