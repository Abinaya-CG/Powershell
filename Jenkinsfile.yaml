pipeline {
  agent  {
   node {
        label 'optim' 
       }
  }
  stages {
    stage('version') {
      steps {
        powershell("$PSVersionTable.PSVersion")
      }
    }
    stage('hello') {
      steps {
        powershell(".'C:/Users/Administrator/Documents/Plutora/optim.ps1'")
      }
    }
  }
  post{
      always{
        sh 'pwsh --version'
      }
   }
}
