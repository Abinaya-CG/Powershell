pipeline {
  agent  {
   node {
        label 'optim' 
       }
  }
  stages {
    stage('version') {
      steps {
        sh 'powershell --version'
      }
    }
    stage('hello') {
      steps {
        powershell hello.ps1
      }
    }
  }
  post{
      always{
        sh 'pwsh --version'
      }
   }
}
