pipeline {
  agent  {
   node {
        label 'optim' 
       }
  }
  stages {
    stage('version') {
      steps {
        powershell '$PSVersionTable.PSVersion'
        def excelFormat = CSVFormat.EXCEL
        def records = readCSV file: 'https://github.com/Abinaya-CG/Powershell/optim_mapping.csv', format: excelFormat
        echo records
      }
    }
    stage('hello') {
      steps {
        powershell 'Write-Output "HI!!"'
      }
    }
  }
}
