pipeline {
  agent any
  stages {
    stage('version') {
      agent {
       node {
        label 'optim' 
       }
      }
      steps {
        sh 'pwsh --version'
      }
    }
    stage('hello') {
      steps {
        sh 'pwsh hello.ps1'
      }
    }
  }
  post{
      always{
        sh 'pwsh --version'
      }
   }
}
