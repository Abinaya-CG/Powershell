pipeline {
  agent  {
   node {
        label 'optim' 
       }
  }
  stages {
    stage('version') {
      steps {
        powershell '$PSVersionTable.PSVersion'
      }
    }
    stage('hello') {
      steps {
        powershell 'Wrote-Output "HI!!"'
      }
    }
  }
}
